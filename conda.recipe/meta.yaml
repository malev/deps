package:
  name: deps
  version: "0.1.0"

build:
  script: python setup.py install

requirements:
  build:
    - python
    - pyyaml
  run:
    - python
    - pyyaml

source:
  path: ../

about:
  home: https://github.com/malev/deps
